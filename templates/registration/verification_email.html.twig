{# Email verification template - sent after registration #}
{# Variables available: signedUrl, expiresAtMessageKey, expiresAtMessageData #}

{# Plain text version (for email clients that don't support HTML) #}
{% block text %}
Witaj w Fiszki AI!

Dziękujemy za rejestrację. Aby dokończyć proces tworzenia konta, potwierdź swój adres email klikając w poniższy link:

{{ signedUrl }}

Link jest ważny przez 1 godzinę.

Jeśli nie rejestrowałeś się w naszym serwisie, zignoruj tę wiadomość.

Pozdrawiamy,
Zespół Fiszki AI
{% endblock %}

{# HTML version (styled email) #}
{% block html %}
<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            padding: 40px 30px;
            text-align: center;
        }
        .header h1 {
            color: #ffffff;
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .content {
            padding: 40px 30px;
        }
        .content p {
            margin: 0 0 20px;
            font-size: 16px;
            color: #555;
        }
        .button-container {
            text-align: center;
            margin: 30px 0;
        }
        .button {
            display: inline-block;
            padding: 16px 40px;
            background-color: #6366f1;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s;
        }
        .button:hover {
            background-color: #4f46e5;
        }
        .info-box {
            background-color: #f0f9ff;
            border-left: 4px solid #3b82f6;
            padding: 16px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .info-box p {
            margin: 0;
            font-size: 14px;
            color: #1e40af;
        }
        .footer {
            background-color: #f9fafb;
            padding: 20px 30px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }
        .footer p {
            margin: 0;
            font-size: 14px;
            color: #6b7280;
        }
        .link {
            color: #6366f1;
            text-decoration: none;
        }
        .small-text {
            font-size: 12px;
            color: #9ca3af;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        {# Header #}
        <div class="header">
            <h1>Witaj w Fiszki AI! 🎓</h1>
        </div>

        {# Content #}
        <div class="content">
            <p><strong>Dziękujemy za rejestrację!</strong></p>

            <p>
                Aby dokończyć proces tworzenia konta i zacząć tworzyć fiszki z pomocą AI,
                potwierdź swój adres email klikając w poniższy przycisk:
            </p>

            {# Verification Button #}
            <div class="button-container">
                <a href="{{ signedUrl }}" class="button">
                    Potwierdź adres email
                </a>
            </div>

            {# Info Box #}
            <div class="info-box">
                <p>
                    <strong>ℹ️ Ważne:</strong> Link aktywacyjny jest ważny przez 1 godzinę.
                    Po tym czasie będziesz musiał zarejestrować się ponownie.
                </p>
            </div>

            <p>
                Jeśli przycisk nie działa, skopiuj i wklej poniższy link do przeglądarki:
            </p>
            <p style="word-break: break-all; font-size: 14px; color: #6366f1;">
                <a href="{{ signedUrl }}" class="link">{{ signedUrl }}</a>
            </p>

            <p style="margin-top: 30px;">
                Jeśli nie rejestrowałeś się w naszym serwisie, po prostu zignoruj tę wiadomość.
            </p>
        </div>

        {# Footer #}
        <div class="footer">
            <p>
                Wiadomość wysłana automatycznie przez <strong>Fiszki AI</strong><br>
                <span class="small-text">Generuj fiszki szybciej dzięki sztucznej inteligencji</span>
            </p>
        </div>
    </div>
</body>
</html>
{% endblock %}
