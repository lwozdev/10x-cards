# .env.test.local - Local overrides for test environment
# Copy this file to .env.test.local and customize for your local setup
# This file should be gitignored for sensitive data

# E2E Test Credentials (customize these)
E2E_USERNAME_ID=test-user-id-123
E2E_USERNAME=test@example.com
E2E_PASSWORD=SecureTestPassword123!

# Base URL for E2E tests (overrides playwright.config.ts default)
# BASE_URL=http://localhost:8000

# Database URL (if different from .env.test)
# DATABASE_URL=postgresql://flashcards_user:flashcards_pass@localhost:6432/flashcards_test?serverVersion=16&charset=utf8

# OpenRouter API Key for integration tests (if needed)
# OPENROUTER_API_KEY=your-test-api-key-here

# Debug settings
# PWDEBUG=1                  # Enable Playwright Inspector
# DEBUG=pw:api               # Enable Playwright debug logs
